# UI -- immer benoetigt
PySide6>=6.6

# CAN -- nur auf dem Pi (real-Backend). Im Mock nicht zwingend importiert
# (real_can importiert 'can' erst beim Start des real-Backends, lazy).
python-can>=4.0 ; platform_machine == "aarch64"

# SPI/BMS -- nur auf dem Pi (real-Backend). Lazy import in real_spi.
spidev>=3.5 ; platform_machine == "aarch64"
